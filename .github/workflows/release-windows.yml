name: Publish Eye Supremo Release

# Pubblica EyeSupremo-Setup.exe unico (Eye Supremo + Ask Fatture) su GitHub Releases.
# I PC lo trovano da Impostazioni - Aggiornamenti.
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Tag versione (es. 1.4.0) senza v'
        required: true
        type: string
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Resolve version
        id: ver
        shell: bash
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            echo "tag=v${{ github.event.inputs.version }}" >> "$GITHUB_OUTPUT"
            echo "name=${{ github.event.inputs.version }}" >> "$GITHUB_OUTPUT"
          else
            RAW="${GITHUB_REF_NAME}"
            echo "tag=${RAW}" >> "$GITHUB_OUTPUT"
            echo "name=${RAW#v}" >> "$GITHUB_OUTPUT"
          fi
      - name: Build Eye Supremo
        shell: powershell
        run: ./scripts/build_windows.ps1
      - name: Build Ask Fatture
        shell: powershell
        run: ./scripts/build_ask_fatture.ps1
      - name: Install Inno Setup if missing
        shell: powershell
        run: |
          $iscc = 'C:\Program Files (x86)\Inno Setup 6\ISCC.exe'
          if (-not (Test-Path $iscc)) { choco install innosetup -y --no-progress }
      - name: Compile unified setup
        shell: powershell
        run: '& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer\EyeSupremo.iss'
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.ver.outputs.tag }}
          name: Eye Supremo ${{ steps.ver.outputs.name }}
          files: release/EyeSupremo-Setup.exe
          generate_release_notes: true
          fail_on_unmatched_files: true
          body: |
            ## Installer unico
            **EyeSupremo-Setup.exe** installa:
            - Eye Supremo (gestionale)
            - Ask Fatture (import XML, catalogo, pack Chili/Litri/Pezzi, Ollama)

            Dati locali: `%LOCALAPPDATA%\EyeSupremo` e `%LOCALAPPDATA%\AskFatture`.
